/* =========================================================
   کافه لوچه — تنظیمات مشترک فرانت‌اند
   ========================================================= */

(() => {
  "use strict";

  // ⚠️ آدرس بک‌اند — اگر سرور روی پورت دیگری است، فقط همین‌جا را عوض کنید
  const API_BASE = "http://localhost:5000";

  window.LuceConfig = {
    API_BASE,
  };
})();
